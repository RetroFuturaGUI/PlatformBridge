# PlatformBridge
A cross-platform API wrapper to be compiled to static libraries

## Usage

### Monitors
####  ```std::vector<MonitorInfo>& PlatformBridge::Monitors::GetMonitors()```
Returns a vector with information of all monitors
